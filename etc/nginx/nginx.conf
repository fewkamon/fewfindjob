http {
    upstream backend {
        server web-backend:5000;
    }

    upstream frontend {
        server web-frontend:3000;
    }

    server {
        listen 80;

        location /api/ {
            proxy_pass http://backend/;
        }

        location / {
            proxy_pass http://frontend/;
        }
    }
}
